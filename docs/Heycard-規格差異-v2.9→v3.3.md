# Heycard 規格差異：GitHub 上一版（v2.9）→ 本次發布（v3.3）

| | |
|---|---|
| 日期 | 2026-08-18 |
| 對象 | 前端／後端／iOS／Android 工程師、QA |
| 基準 | 上一版 GitHub Pages（原型 v2.9，對應《工程規格書 v2.0》） |
| 本版 | GitHub Pages 現行（原型 v3.3，commit `89183fa`） |
| 用法 | 《工程規格書 v2.0》仍是主文件；**本文列出的每一條都覆蓋 v2.0 對應章節**。新編號用 v2.0 的前綴，尾碼從 50 起（例：`PAY-50`），避免與舊編號衝突。 |

畫面數：47 → **53**（新增 `camMine`、`checkout`、`subDone`、`seasonList`、`proposal`、`language`）。全畫面自動巡檢：53 開啟／0 壞／0 頁面錯誤。

---

## 1. 殼層與響應式（新章節，補 v2.0 §2／§3）

| 編號 | 需求 | 對照 v2.0 |
|---|---|---|
| `SHELL-50` | 同一套 SCREENS／路由／資料，兩種殼：**< 900px 手機殼**（底部 5 格＋中央拍照鍵、模擬狀態列）；**≥ 900px 桌機殼**：左側 244px 直欄導覽（Logo、尋求、人脈、拍名片、訊息、名片、方案卡、設定），中欄內容最寬 600px 置中、左右細線；無手機邊框、無狀態列、無切頁動畫。 | 新增（v2.0 只有手機） |
| `SHELL-51` | ≥ 1360px 右欄：我的名片預覽（200px 寬）、姓名／職稱、設計／分享／公開頁三鈕、依方案顯示升級模組（Free→Plus、Plus→Pro、Pro→感謝）。 | 新增 |
| `SHELL-52` | 桌機側欄與右欄在每次路由變化後重繪（Tab 高亮、名片、方案狀態）；未登入時桌機為置中單欄、無側欄。 | 新增 |
| `SHELL-53` | Sheet、Toast 在桌機對齊中欄（left 244px、max-width 600px）。 | 新增 |
| `NAV-01` 修訂 | 桌機側欄的「拍名片」＝手機中央鍵；「設定」放側欄底部。 | 修訂 |

## 2. 全站視覺收斂（補 v2.0 §1 PRIN）

`PRIN-50`：段落間距 32/14、卡片圓角 16、晶片改膠囊（radius 99）、列高 11px 上下、標題 16.5px、按鈕圓角 12。`PRIN-51`：**PLUS／PRO 徽章 `tierPill`** 全站唯一長相（PLUS 淺錳藍底、PRO 深底金字、未擁有時帶鎖圖示、已擁有時青色底）。

## 3. 付費動線（覆蓋 v2.0 §16）

| 編號 | 需求 | 對照 v2.0 |
|---|---|---|
| `PAY-50` | **付款在網頁完成，不經 App Store**：方案頁 CTA → `checkout`（heycard.app／checkout 樣式：明細、NFC 贈品列、下次扣款、付款方式 信用卡／Apple Pay／LINE Pay、註明不經 App Store）→ 付款成功 → `subDone` 開通儀式頁（Pro 深色；顯示升級後的自己名片；去設計名片／回到名片）。App 內按「訂閱」開外部瀏覽器到 checkout；完成後 deep link 回 App。 | 取代 §16.3 `PAY-02`「原型直接寫入 plan」；正式版 IAP 改為**網頁金流為主**（`PAY-03` 訂閱狀態以 server 為準不變）。⚠️ iOS 外部購買規則需法務確認（見 §22 新增第 9 項） |
| `PAY-51` | **付費選項可試、套用時才升級**：設計器裡 Plus／Pro 選項全部可點，預覽即時套用在自己名片；預覽下方顯示「用到 PLUS／PRO」徽章；右上按鈕文字變「升級並套用」；按下時免費部分先存、付費部分彈升級面板。 | 取代 `PAY-01`「點擊當下攔截」與 v2.0 §7.6 `THEME-07`「點選款式即攔」 |
| `PAY-52` | **全站唯一升級面板 `upsell(need, why)`**：底部 sheet＝徽章＋一句為什麼＋「現在→升級後」兩張自己的名片並排（升級後示範值：Plus 去標記＋色調；Pro 再加字色＋字體）＋三行好處＋一顆 CTA（看 Plus／Pro，價格）＋「先看看就好」。`paywall()` 一律轉進 `upsell()`。 | 取代 `paywall` 直接跳方案頁 |
| `PAY-53` | **方案頁重做**：上方 Free／Plus／Pro 三段切換，預覽用自己名片升級後的樣子（210px）；一句標語（目前方案加註）；該層功能列（勾＋一句）；年繳／月繳（年繳＝10 個月價，顯示「省 2 個月」，Pro 加「送 NFC 卡」）；一顆 CTA（升級／改為／使用中／已包含）＋「相當於每月」；精簡比較表（7 列，當前選中欄反白）；頁尾「訂閱在 heycard.app 完成，不經 App Store」。 | 取代 §16 方案頁 |
| `PAY-54` | 版式與 Logo 位置**免費**；Plus 鎖：去 Heycard 標記、色調、含色調的款式；Pro 鎖：字色、字體、洞察、AI 額度、提案。 | 修訂 §16.2 矩陣（版式列改為三層皆 ✅；新增「商業提案 Pro」列） |
| `PAY-55` | 名片頁分享鈕下的三個文字連結改三顆等寬圖示按鈕（編輯／設計／預覽）；下方一條「設計狀態帶」：免費款／Plus 設計／Pro 設計 ＋ 升級提示。設定頁訂閱列掛徽章。 | 新增 |

## 4. 名片主題／設計器（覆蓋 v2.0 §7）

`THEME-50`：設計器重寫成單一元件；分頁列以細線分三段：**款式 材質 版式 Logo ｜ 色調 標記 ｜ 字色 字體**，付費分頁掛徽章（已擁有則不顯示）；付費分頁內容頂端一條「先在自己的名片上試，喜歡再升級帶走」。`THEME-51`：款式磁磚右上角掛徽章；PRESETS 調整：極光／錳藍改為免費（font modern；錳藍 layout minimal），其餘不變。`THEME-52`：色調色票在灰系材質用 `mix-blend-mode:color` 疊層（與名片一致）。

## 5. 相機與建卡（覆蓋 v2.0 §11、§17）

| 編號 | 需求 |
|---|---|
| `SCAN-50` | 新畫面 **`camMine`**（拍自己的名片）：頂列 ✕／步驟指示（● 正面 — 反面）／開相機；取景框；快門列（旋轉／快門／略過）。流程：拍正面 → 框內顯示縮圖＋勾、重拍或「下一步：拍反面」→ 拍反面（可略過）→ 「開始辨識」→ 辨識進度頁（兩張縮圖＋進度條）→ `cardEdit`（欄位預填、頂端「已從名片辨識帶入，看一眼有沒有錯字」提示）。`cardMode` 的「拍實體名片」改為進此畫面（原本跳手動輸入）。 |
| `SCAN-51` | **取景框自動判斷橫直向**（`camFrameHTML` 共用元件，`camera`／`camMine` 皆用）：開啟預設直框，偵測到橫向 → 框以 320ms 轉成橫框（寬 80%、1.586:1）＋頂部提示「偵測到橫向名片，取景框已轉向」；「旋轉」鈕手動覆蓋後不再自動改。**正式版演算法**：有影像串流時每 300ms 取幀 → 邊緣偵測 → 最大四邊形 bbox；寬/高 ≥ 1.2 橫、≤ 0.83 直、之間維持上一判斷（去抖）；無結果前預設橫向。 |
| `SCAN-52` | 取景框：四角 L 形白線、框外 50% 黑遮罩、框下方膠囊提示文字（隨框位置移動）。 |
| `AUTH-50` | `cardMode` 改為兩張大磁磚（深色相機磁磚含取景框插圖；鍵盤磁磚），文字只剩標題與一句。 |
| `CARD-50` | `cardEdit` 接受 `a.prefill{}`：載入後把值填入 `[data-k]` 並觸發 input。 |

## 6. 洞察（覆蓋 v2.0 §14）

| 編號 | 需求 |
|---|---|
| `INS-50` | **鐵則：每一條洞察都附下一步**（可按的動作）。空狀態也附「發需求」。 |
| `INS-51` | 新段落「**時機**」（AI）：規則表 `SEASONS[{m 月份, t 標題, w 一句時效, who 對象條件(level/func/industry/ticker/tier), me 受益方關鍵字, role 預填角色, act compose|hello}]`；台灣版六波：1–2 月年初人事預算、3–4 月 Q1 結算展會、5–6 月股東會季（上市櫃）、7–9 月 Q3 底編明年預算（決策層＋行銷／採購／經營／策略）、10–11 月 Q4 電商零售衝刺、12 月年終問候（所有人）。 |
| `INS-52` | **相關性判讀（AI 邏輯）**：`relevance` ＝ 我的專長是否接得上（名片 offer／industry／title／headline 對照 `me` 清單，硬門檻，接不上不顯示）× 人脈中符合對象人數 × 時效（離該波最後一月越近權重越高：0/1/2+ 月 → 1/.7/.4）；最多顯示 2 張。名片沒填 offer／industry／headline 時不猜，顯示「先告訴 AI 你能提供什麼」引導卡（點入欄位編輯）；專長無關時一句「AI 就不打擾你」。每張卡右上角「因為你的『行銷』」晶片（可解釋性）。正式版 LLM 用同一組輸入與門檻，地區化規則表由 LLM 依市場產生。 |
| `INS-53` | 時機卡結構：月份徽章＋標題＋因為晶片／一句時效／對象頭像疊＋人數／兩顆動作：發需求（`compose` 帶 `role` 預填）或打招呼（`draft hello`）＋看這 N 位（新畫面 `seasonList`：清單，每列可打招呼）。 |
| `INS-54` | 「你的網路」維度切換**就地重畫**（`#dimTabs`／`#dimBox`），不重整頁面、不跳頂；每個維度圖表下方一句判斷＋一顆按鈕（價值：沉睡 N 位→從某人開始；產業：最深產業→向這個圈子發需求；層級：決策層 N 位→看某人／沒有→發需求；活躍：N 位沒說過話→跟某人說第一句）。 |
| `INS-55` | 「樞紐」改「**引路人**」：三張人物磁磚並排（環形頭像、名字、公司簡稱、1–3 個圖示徽章：皇冠＝決策層、鑰匙＝唯一窗口、放大鏡＝正在找人、一顆「引薦」→ `draft ask`）＋一行圖例；**不再有說明句**。 |
| `INS-56` | 「本週變化」每列尾端 CTA 文字（看動態／幫他引薦／打招呼 →）。 |
| `INS-57` | Pro 鎖改用徽章＋upsell（模糊真實內容＋一張卡）。 |

## 7. 商業提案（新，Pro）

| 編號 | 需求 |
|---|---|
| `PROP-50` | 人脈詳頁「合作機會」下方一張深色卡「為 {name} 寫提案」＋PRO 徽章；非 Pro 點擊 → upsell('pro')。 |
| `PROP-51` | 新畫面 **`proposal`**：頂部對方摘要＋PRO 徽章；標題（可改）；四段可編輯：我看到的機會（對方公司情報 desc＋他公開的需求＋當前時機＋合作點）、我能提供（我的 offer＋headline；沒填則由公司／職稱／對方產業組一句）、合作方式（30 分鐘會議→兩週方案報價→小範圍試行）、下一步（約時間）；底部：複製全文、AI 重寫；右上「傳給他」→ 寫入該人對話（用戶）或存到對話待寄信（非用戶）。 |
| `PROP-52` | 正式版 LLM 輸入契約：我的名片（offer／headline／title／company）、對方名片、對方公司情報（登記＋官網摘要＋產品）、對方公開需求、當前時機、我們的往來（僅「你在場的事」）；輸出固定四段。 |

## 8. 人脈列表與詳頁（覆蓋 v2.0 §10）

| 編號 | 需求 |
|---|---|
| `NET-50` | **Heycard 用戶 vs 非用戶的視覺**：用戶頭像加錳→青漸層細環（2px＋白間隙）、名字旁勾、第三行永遠有活資訊（正在找／可以提供／「本人維護 · 資料即時」）並帶青色小點、右側**訊息鈕**（直接開對話）；非用戶頭像灰階字首磚（grayscale .35）、右側箭頭。列外層改 `div[role=button]` 以容納內層按鈕。 |
| `NET-51` | 用戶詳頁：名片下方「活資訊帶」晶片（本人維護 · 更新時間／N 個身分／公開頁 →），主行動三顆：傳訊息（主）＋AI 開場＋引薦。非用戶維持原樣（邀請卡）。 |
| `NET-52` | 首頁「時機」等處用 `data-compose-role` 進 `compose` 時 `#role` 預填。 |

## 9. 多語（新章節）

| 編號 | 需求 |
|---|---|
| `I18N-50` | **語言判斷照 IG／FB**：預設跟裝置／瀏覽器語言（`navigator.language` zh-* → 中文，其他 → English），不問人；設定 → 語言 → 新畫面 `language`：清單第一項「裝置語言（預設）」（顯示目前偵測到的語言），下面中文（繁體）、English，日本語／한국어灰掉標「即將推出」；選了覆蓋、可改回預設。設定頁語言列顯示目前選項。 |
| `I18N-51` | Web 額外吃 `?hl=en|zh` 一次性覆蓋。`<html lang>` 跟著改。 |
| `I18N-52` | 原型作法：渲染後翻譯——MutationObserver 盯 `#dev`，文字節點／placeholder／title／aria-label 依字典（完整字串比對，全形空白正規化）＋含變數規則（約 60 條 regex，群組遞迴翻譯）。字典裡沒有的（人名、公司名、使用者輸入）保留。**正式版**：key-based（FBT／ICU），`en.json` 以原型字典（約 520 條）為第一版；RTL 預留 `dir`。 |
| `I18N-53` | 英文較長 20–40%：標題允許換行、列文字截斷、分頁列可橫捲——主要 20 個畫面已截圖驗過無爆版。 |

## 10. 資料模型變更（覆蓋 v2.0 §5）

- Card：無新增欄位（`hideBrand/hue/ink/font` 語意不變）。
- Contact：新增可選 `updated`（最後更新時間，用戶顯示於活資訊帶）。
- Storage：新增 `hc_lang`（'' 表示跟裝置）。
- 新常數：`SEASONS`（時機規則）、`TIER_FEATS`（方案功能列）、`LANGS`。
- 訂閱：`checkout` 成功才 `setPlan`；`subDone` 顯示 `plan/cycle`。

## 11. 需產品決策（追加到 v2.0 §22）

9. **iOS 外部付款**：Apple 對導向外部購買的規則（外部連結 entitlement、不得在 App 內做價格比較）——上架前法務／上架顧問確認。
10. 時機規則表的地區化：go global 時每個市場一份（美國 Q4 財年末、日本 4 月年度始）；需要「地區」欄位。
11. 英文名片字體：英文名字是否改 Manrope／Inter 系。
12. 引路人磁磚上的圖示是否需要一次性教學（目前用圖例）。

## 12. 驗收追加

- [ ] ≥ 900px 出現側欄、中欄 600px；≥ 1360px 出現右欄；< 900px 為手機殼。
- [ ] 設計器：Free 用戶可點色調／字色並看到預覽；套用 → 免費部分存、彈升級面板；面板兩張卡都是自己的名片。
- [ ] 方案頁三段切換預覽用自己名片；CTA → checkout → 付款 → subDone → 方案生效。
- [ ] `cardMode` 拍實體名片 → 正面／反面／辨識 → cardEdit 預填；取景框開啟 0.7s 內轉橫並提示；旋轉可手動覆蓋。
- [ ] 洞察：時機只在專長相關時出現、卡片有「因為你的…」；維度切換不跳頂；引路人三磁磚＋圖示；每段都有可按的下一步。
- [ ] Pro：提案畫面四段可改、傳給他寫入對話；非 Pro 走升級面板。
- [ ] 人脈列：用戶環形頭像＋訊息鈕；非用戶灰階＋箭頭。
- [ ] 語言：瀏覽器非中文自動英文；設定 → 語言 → 裝置語言／中文／English；`?hl=en` 生效；主要畫面英文不爆版。
- [ ] 全畫面巡檢 53／0 壞／0 錯誤。
